import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'zustand-persist-capacitor',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
